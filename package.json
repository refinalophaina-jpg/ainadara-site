{
  "name": "ainadara-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "AinaDara.com — personal brand hub (Astro static site).",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --test test/access-gate.test.mjs test/gate-function.test.mjs"
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.6.0"
  }
}
